[project]
name = "codex2api"
version = "0.1.0"
description = "GPT-image-2 Web reverse proxy with dashboard-managed Codex OAuth accounts and OpenAI-compatible image APIs."
requires-python = ">=3.10"

license = "AGPL-3.0-only"
[project.scripts]
codex-image = "codex_image.cli:main_entry"
